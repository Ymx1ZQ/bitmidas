1) data are cleaned using cleanData.m which fills the missing values and averages each minute
2) estimations and forecasts are performed in performForecasts.m which saves the data into computations.mat
3) strategies are evaluated in evaluateStrategies.m
4) strategies are stored in files named as chooseXXX_description.m